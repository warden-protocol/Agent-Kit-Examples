import { WardenAction } from "./warden_action";
import { Account } from "viem";
import { z } from "zod";
/**
 * Input schema for create space action.
 */
export declare const CreateSpaceInput: z.ZodObject<{}, "strip", z.ZodTypeAny, {}, {}>;
/**
 * Creates a new space with the given name.
 *
 * @param account - The account creating the space.
 * @returns A message confirming the space creation.
 */
export declare function createSpace(account: Account, args: z.infer<typeof CreateSpaceInput>): Promise<string>;
/**
 * Create space action.
 */
export declare class CreateSpaceAction implements WardenAction<typeof CreateSpaceInput> {
    name: string;
    description: string;
    schema: z.ZodObject<{}, "strip", z.ZodTypeAny, {}, {}>;
    function: typeof createSpace;
}
