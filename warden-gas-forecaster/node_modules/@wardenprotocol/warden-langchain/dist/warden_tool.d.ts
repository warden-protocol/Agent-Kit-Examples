import { StructuredTool } from "@langchain/core/tools";
import { WardenAgentKit, WardenAction, WardenActionSchemaAny } from "@wardenprotocol/warden-agent-kit-core";
import { z } from "zod";
export declare class WardenTool<TActionSchema extends WardenActionSchemaAny> extends StructuredTool {
    schema: TActionSchema;
    name: string;
    description: string;
    private agentkit;
    private action;
    constructor(action: WardenAction<TActionSchema>, agentkit: WardenAgentKit);
    protected _call(input: z.infer<typeof this.schema> & Record<string, unknown>): Promise<string>;
}
