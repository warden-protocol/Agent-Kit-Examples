import type { Account, Hex } from "viem";
import { WardenAction, WardenActionSchemaAny } from "./actions/warden/warden_action";
/**
 * Configuration options for the Warden Agent Kit
 */
interface WardenAgentKitOptions {
    privateKeyOrAccount: Hex | Account;
}
/**
 * Warden Agent Kit
 */
export declare class WardenAgentKit {
    private account;
    constructor({ privateKeyOrAccount }: WardenAgentKitOptions);
    run<TActionSchema extends WardenActionSchemaAny>(action: WardenAction<TActionSchema>, args: TActionSchema): Promise<string>;
}
export {};
