import { WardenAction } from "./warden_action";
import { Account } from "viem";
import { z } from "zod";
/**
 * Input schema for create order action.
 */
export declare const CreateOrderInput: z.ZodObject<{}, "strip", z.ZodTypeAny, {}, {}>;
/**
 * Creates a new order in the marketplace.
 *
 * @param account - The account creating the order.
 * @param args - The input arguments for the action.
 * @returns A message containing the order creation status.
 */
export declare function createOrder(account: Account, args: z.infer<typeof CreateOrderInput>): Promise<string>;
/**
 * Create order action.
 */
export declare class CreateOrderAction implements WardenAction<typeof CreateOrderInput> {
    name: string;
    description: string;
    schema: z.ZodObject<{}, "strip", z.ZodTypeAny, {}, {}>;
    function: typeof createOrder;
}
