import { WardenAction } from "./warden_action";
import { Account } from "viem";
import { z } from "zod";
/**
 * Input schema for get spaces action.
 */
export declare const GetSpacesInput: z.ZodObject<{}, "strip", z.ZodTypeAny, {}, {}>;
/**
 * Gets spaces for a given account.
 *
 * @param account - The account to get the spaces for.
 * @returns A message containing the spaces information.
 */
export declare function getSpaces(account: Account, args: z.infer<typeof GetSpacesInput>): Promise<string>;
/**
 * Get spaces action.
 */
export declare class GetSpacesAction implements WardenAction<typeof GetSpacesInput> {
    name: string;
    description: string;
    schema: z.ZodObject<{}, "strip", z.ZodTypeAny, {}, {}>;
    function: typeof getSpaces;
}
